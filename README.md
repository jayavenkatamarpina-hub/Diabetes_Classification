# Diabetes_Classification
Comparing 5 classification models (Logistic Regression, KNN, Decision Tree, Naive Bayes, SVM) to predict diabetes, evaluated with accuracy, precision, recall, F1-score and confusion matrix.
